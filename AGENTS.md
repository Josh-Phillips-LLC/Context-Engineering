# Runtime Instructions Loader

Use the canonical runtime role instructions at:

- /workspace/instructions/role-instructions.md

If the canonical runtime file is unavailable, continue with repository-governed instructions and report the missing runtime file.
